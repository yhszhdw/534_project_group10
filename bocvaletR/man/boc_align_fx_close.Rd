% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boc_utils.R
\name{boc_align_fx_close}
\alias{boc_align_fx_close}
\title{Align FX/IR timestamps to a common "close" date}
\usage{
boc_align_fx_close(
  data,
  datetime_col = "timestamp",
  input_tz = "UTC",
  cutoff_hour = 17,
  cutoff_min = 0,
  cutoff_tz = "America/New_York",
  new_col = "fx_date"
)
}
\arguments{
\item{data}{A data.frame/tibble containing the time series.}

\item{datetime_col}{Name of the POSIXct timestamp column (string).}

\item{input_tz}{Time zone used to interpret \code{datetime_col} if it is stored as
character (default "UTC").}

\item{cutoff_hour}{Integer hour (0-23) of the daily close in \code{cutoff_tz}.}

\item{cutoff_min}{Integer minute (0-59) of the daily close.}

\item{cutoff_tz}{Olson time zone string for the close (default "America/New_York").}

\item{new_col}{Name of the output date column to create (string). Default "fx_date".}
}
\value{
A tibble/data.frame with an added date column \code{new_col} that buckets
each observation to the chosen close date.
}
\description{
Exchange rate and interest-rate markets trade across time zones. To avoid
misaligned daily changes (e.g., NY close vs. London close), align each
timestamp to a synthetic "close" date based on a cut-off time in a reference
time zone (default 17:00 America/New_York, the common FX convention).
}
